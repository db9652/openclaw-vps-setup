# HEARTBEAT.md — Proactive Checks

# Add tasks below for periodic checks (runs every ~30 min on a cheap model).
# Keep it lightweight to minimize token burn.

# Example:
# - Check if daily log exists for today; create if missing
# - Summarize yesterday's log if not done
# - Flag any workspace file over 2000 tokens
